name: Queue Smoke Test

"on":
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/queue-smoke-test.yml"

jobs:
  smoke:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Queue check
        run: echo "GitHub Actions queue is working"
